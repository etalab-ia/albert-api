rate_limit:
  by_key: 300/minute
  by_ip: 1000/minute

databases:
  - type: sql
    args:
      url: "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}"

  - type: redis
    args:
      host: ${REDIS_HOST}
      port: 6379
      password: ${REDIS_PASSWORD}

  - type: qdrant
    args:
      url: http://${QDRANT_HOST}:6333
      api_key: ${QDRANT_API_KEY}
      prefer_grpc: True
      grpc_port: 6334
      timeout: 10

  - type: grist
    args:
      api_key: ${GRIST_API_KEY}
      server: https://grist.numerique.gouv.fr
      doc_id: ${GRIST_DOC_ID}
      table_id: DEV

models:
  - id: meta-llama/Llama-3.1-8B-Instruct
    type: text-generation
    owned_by: test
    default_internet: true
    aliases: ["AgentPublic/Llama-3.1-8B-Instruct", "AgentPublic/llama3-instruct-8b"]
    clients:
      - model: meta-llama/Llama-3.1-8B-Instruct
        type: albert
        args:
          api_url: https://albert.api.etalab.gouv.fr
          api_key: ${ALBERT_API_KEY}

  - id: BAAI/bge-m3
    type: text-embeddings-inference
    default_internet: true
    aliases: ["BAAI/bge-m3-large"]
    clients:
      - model: BAAI/bge-m3
        type: albert
        args:
          api_url: https://albert.api.etalab.gouv.fr
          api_key: ${ALBERT_API_KEY}

  - id: openai/whisper-large-v3
    type: automatic-speech-recognition
    clients:
      - model: openai/whisper-large-v3
        type: albert
        args:
          api_url: https://albert.api.etalab.gouv.fr
          api_key: ${ALBERT_API_KEY}

  - id: BAAI/bge-reranker-v2-m3
    type: text-classification
    clients:
      - model: BAAI/bge-reranker-v2-m3
        type: albert
        args:
          api_url: https://albert.api.etalab.gouv.fr
          api_key: ${ALBERT_API_KEY}